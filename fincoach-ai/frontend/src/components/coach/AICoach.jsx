import React from 'react';
import { COLORS } from '../../styles/theme';

const AICoach = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>AICoach Component</h3>
    </div>
  );
};

export default AICoach;
