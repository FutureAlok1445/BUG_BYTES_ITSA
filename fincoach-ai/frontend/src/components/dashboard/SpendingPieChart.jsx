import React from 'react';
import { COLORS } from '../../styles/theme';

const SpendingPieChart = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>SpendingPieChart Component</h3>
    </div>
  );
};

export default SpendingPieChart;
