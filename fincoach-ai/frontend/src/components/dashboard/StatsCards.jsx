import React from 'react';
import { COLORS } from '../../styles/theme';

const StatsCards = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>StatsCards Component</h3>
    </div>
  );
};

export default StatsCards;
