import React from 'react';
import { COLORS } from '../../styles/theme';

const GoalsTracker = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>GoalsTracker Component</h3>
    </div>
  );
};

export default GoalsTracker;
