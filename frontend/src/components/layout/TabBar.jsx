import React from 'react';
import { COLORS } from '../../styles/theme';

const TabBar = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>TabBar Component</h3>
    </div>
  );
};

export default TabBar;
