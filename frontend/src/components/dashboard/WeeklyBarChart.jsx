import React from 'react';
import { COLORS } from '../../styles/theme';

const WeeklyBarChart = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>WeeklyBarChart Component</h3>
    </div>
  );
};

export default WeeklyBarChart;
