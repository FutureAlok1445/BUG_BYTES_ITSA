import React from 'react';
import { COLORS } from '../../styles/theme';

const AlertCard = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>AlertCard Component</h3>
    </div>
  );
};

export default AlertCard;
