import React from 'react';
import { COLORS } from '../../styles/theme';

const ForecastBar = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>ForecastBar Component</h3>
    </div>
  );
};

export default ForecastBar;
