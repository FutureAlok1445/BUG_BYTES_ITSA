import React from 'react';
import { COLORS } from '../../styles/theme';

const PersonalityCard = () => {
  return (
    <div style={{ padding: '1rem', border: `1px solid ${COLORS.border}` }}>
      <h3>PersonalityCard Component</h3>
    </div>
  );
};

export default PersonalityCard;
